import { ChatInputCommandInteraction } from "discord.js";

export default class CustomChatInteraction extends ChatInputCommandInteraction {
    constructor(interaction: ChatInputCommandInteraction) {
        super()
        
    }
}