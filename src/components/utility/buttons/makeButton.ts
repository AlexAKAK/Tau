
/*import { MessageButton, MessageButtonStyleResolvable } from "discord-buttons"

const disbut = require('discord-buttons')

export default function(color: MessageButtonStyleResolvable, label: string, id: string, ) {
    let button: MessageButton = new disbut.MessageButton()
    button.setStyle(color)
    button.setLabel(label)
    button.setID(id)

    return button

}
*/