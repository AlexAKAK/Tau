import { Message } from "discord.js";
import src from "../../..";
import CommandClass from "../../classes/CommandClass";

export default class price extends CommandClass {
    public commandMain(message: Message<boolean>, client: src): Promise<void> {
        
    }
    
}